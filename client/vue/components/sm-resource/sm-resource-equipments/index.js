import SmResourceEquipments from './SmResourceEquipments';

SmResourceEquipments.install = function(Vue) {
  Vue.component(SmResourceEquipments.name, SmResourceEquipments);
};

export default SmResourceEquipments;
