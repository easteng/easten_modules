import SmResourceCodeGeneration from './SmResourceCodeGeneration';

SmResourceCodeGeneration.install = function (Vue) {
  Vue.component(SmResourceCodeGeneration.name, SmResourceCodeGeneration);
};

export default SmResourceCodeGeneration;
