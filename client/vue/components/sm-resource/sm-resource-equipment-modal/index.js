import SmResourceEquipmentModal from './SmResourceEquipmentModal';

SmResourceEquipmentModal.install = function(Vue) {
  Vue.component(SmResourceEquipmentModal.name, SmResourceEquipmentModal);
};

export default SmResourceEquipmentModal;
