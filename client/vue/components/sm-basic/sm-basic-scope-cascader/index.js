import SmBasicScopeCascader from './SmBasicScopeCascader';

SmBasicScopeCascader.install = function (Vue) {
  Vue.component(SmBasicScopeCascader.name, SmBasicScopeCascader);
};

export default SmBasicScopeCascader;
